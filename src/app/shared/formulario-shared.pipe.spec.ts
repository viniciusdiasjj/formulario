import { FormularioSharedPipe } from './formulario-shared.pipe';

describe('FormularioSharedPipe', () => {
  it('create an instance', () => {
    const pipe = new FormularioSharedPipe();
    expect(pipe).toBeTruthy();
  });
});
