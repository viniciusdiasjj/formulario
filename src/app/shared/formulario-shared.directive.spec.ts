import { FormularioSharedDirective } from './formulario-shared.directive';

describe('FormularioSharedDirective', () => {
  it('should create an instance', () => {
    const directive = new FormularioSharedDirective();
    expect(directive).toBeTruthy();
  });
});
