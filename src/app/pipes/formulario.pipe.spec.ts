import { FormularioPipe } from './formulario.pipe';

describe('FormularioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormularioPipe();
    expect(pipe).toBeTruthy();
  });
});
