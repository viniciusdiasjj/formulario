import { FormularioDirective } from './formulario.directive';

describe('FormularioDirective', () => {
  it('should create an instance', () => {
    const directive = new FormularioDirective();
    expect(directive).toBeTruthy();
  });
});
